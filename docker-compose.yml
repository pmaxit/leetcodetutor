version: '3.8'

services:
  app:
    build: .
    ports:
      - "3005:8080"
    env_file:
      - .env
    environment:
      - NODE_ENV=development
      - PORT=8080
      - LLM_ENABLE_TOOLS=false
    restart: unless-stopped
